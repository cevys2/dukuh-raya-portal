# URL of the Portal's own backend (leave as /api to use the dev proxy in vite.config.ts)
VITE_PORTAL_API_URL=/api

# Comma-separated list of app origins allowed to receive a login token via
# the post-login `redirect` bounce-back (open-redirect / token-leak guard).
VITE_ALLOWED_APP_ORIGINS=http://localhost:5173
