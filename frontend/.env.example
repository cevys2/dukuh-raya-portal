# URL of shipyard-pricing's existing backend, used only for /auth/login
VITE_SHIPYARD_API_URL=http://localhost:8000

# URL of the Portal's own backend (leave as /api to use the dev proxy in vite.config.ts)
VITE_PORTAL_API_URL=/api
